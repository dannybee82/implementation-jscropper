import { Routes } from '@angular/router';
import { CropperPageComponent } from './components/cropper-page/cropper-page.component';

export const routes: Routes = [
    {
        path: '',
        component: CropperPageComponent
    }
];